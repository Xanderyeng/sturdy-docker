version: '3.5'
services:
  mysql:
    image: benlumia007/mysql:latest
    container_name: docker-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    command: --default-authentication-plugin=mysql_native_password
    volumes:
       - db_data:/var/lib/mysql
    ports:
      - "3306:3306"

  nginx:
    image: benlumia007/nginx:latest
    container_name: docker-nginx
    restart: always
    depends_on:
      - dashboard
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./config/nginx:/etc/nginx/conf.d
      - ./certificates:/etc/nginx/certs
      - ./sites/dashboard/public_html:/var/www/html/dashboard/public_html
      - ./sites/sandbox/public_html:/var/www/html/sandbox/public_html

  mailhog:
    image: mailhog/mailhog
    container_name: docker-mailhog
    restart: always
    ports:
      - "1025:1025"
      - "8025:8025"

  dashboard:
    image: benlumia007/php:7.3-fpm
    container_name: docker-dashboard
    restart: always
    ports:
      - "9000"
    volumes:
      - ./sites/dashboard/public_html:/var/www/html/dashboard/public_html
      - ./config/docker-custom.yml:/var/www/html/dashboard/public_html/config/docker-custom.yml

  sandbox:
    image: benlumia007/php:7.3-fpm
    container_name: docker-sandbox
    restart: always
    ports:
      - "9000"
    volumes:
      - ./sites/sandbox/public_html:/var/www/html/sandbox/public_html

volumes:
  db_data: